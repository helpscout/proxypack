diff a/src/proxyServer/index.js b/src/proxyServer/index.js	(rejected hunks)
@@ -57 +57 @@ const proxyServer = hoxy
-proxyServer.log('error', function (event) {
+proxyServer.log('error', function(event) {
@@ -72 +72 @@ module.exports = {
-    withRpcServer = false
+    withRpcServer = false,
@@ -84,3 +84,4 @@ module.exports = {
-      withRpcServer && rpcServer.init({
-        onExternalMappingsChange: state.setExternalMappings
-      })
+      withRpcServer &&
+        rpcServer.init({
+          onExternalMappingsChange: state.setExternalMappings,
+        })
